name: layer-storpool-openstack-integration
summary: Install the OpenStack integration into all the LXD containers
maintainer: StorPool <support@storpool.com>
description: |
  This layer installs a Debian package and runs the StorPool OpenStack
  integration modules into the main system ("the bare metal") and all
  the LXD containers.
